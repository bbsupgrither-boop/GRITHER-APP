РїВ»С—import { Home, Trophy, CheckSquare, ShoppingCart, Users, Zap, Gamepad2, Box } from './Icons';

interface AdminNavigationProps {
  currentAdminPage: string;
  onNavigate: (page: string) => void;
}

export function AdminNavigation({ currentAdminPage, onNavigate }: AdminNavigationProps) {
  const adminPages = [
    {
      id: 'dashboard',
      label: 'Р В РІР‚СљР В Р’В»Р В Р’В°Р В Р вЂ Р В Р вЂ¦Р В Р’В°Р РЋР РЏ',
      icon: Home
    },
    {
      id: 'workers',
      label: 'Р В РІР‚в„ўР В РЎвЂўР РЋР вЂљР В РЎвЂќР В Р’ВµР РЋР вЂљР РЋРІР‚в„–',
      icon: Users
    },
    {
      id: 'battles',
      label: 'Р В РІР‚ВР В Р’В°Р РЋРІР‚С™Р РЋРІР‚С™Р В Р’В»Р РЋРІР‚в„–',
      icon: Zap
    },
    {
      id: 'achievements',
      label: 'Р В РІР‚СњР В РЎвЂўР РЋР С“Р РЋРІР‚С™Р В РЎвЂР В Р’В¶Р В Р’ВµР В Р вЂ¦Р В РЎвЂР РЋР РЏ', 
      icon: Trophy
    },
    {
      id: 'tasks',
      label: 'Р В РІР‚вЂќР В Р’В°Р В РўвЂР В Р’В°Р РЋРІР‚РЋР В РЎвЂ',
      icon: CheckSquare
    },
    {
      id: 'shop',
      label: 'Р В РЎС™Р В Р’В°Р В РЎвЂ“Р В Р’В°Р В Р’В·Р В РЎвЂР В Р вЂ¦',
      icon: ShoppingCart
    },
    {
      id: 'games',
      label: 'Р В Р’ВР В РЎвЂ“Р РЋР вЂљР РЋРІР‚в„–',
      icon: Gamepad2
    },
    {
      id: 'cases',
      label: 'Р В РЎв„ўР В Р’ВµР В РІвЂћвЂ“Р РЋР С“Р РЋРІР‚в„–',
      icon: Box
    }
  ];

  return (
    <nav className="glass-card border-t border-border/50 backdrop-blur-xl">
      <div className="grid grid-cols-4 p-3">
        {adminPages.slice(0, 4).map((page) => {
          const Icon = page.icon;
          
          return (
            <button
              key={page.id}
              onClick={() => onNavigate(page.id)}
              className="flex flex-col items-center justify-center p-2 text-muted-foreground hover:text-foreground transition-colors"
            >
              <Icon className="w-5 h-5" />
              <span className="text-xs mt-1">{page.label}</span>
            </button>
          );
        })}
      </div>
      <div className="grid grid-cols-4 p-3 pt-0">
        {adminPages.slice(4, 8).map((page) => {
          const Icon = page.icon;
          
          return (
            <button
              key={page.id}
              onClick={() => onNavigate(page.id)}
              className="flex flex-col items-center justify-center p-2 text-muted-foreground hover:text-foreground transition-colors"
            >
              <Icon className="w-5 h-5" />
              <span className="text-xs mt-1">{page.label}</span>
            </button>
          );
        })}
      </div>
    </nav>
  );
}
