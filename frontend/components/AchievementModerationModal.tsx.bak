РїВ»С—import { useState } from 'react';
import { CheckSquare, Paperclip } from './Icons';
import { ModalOpaque } from './ModalOpaque';

interface AchievementSubtask {
  id: string;
  description: string;
  requiresAttachment: boolean;
  completed: boolean;
  hasAttachment: boolean;
  attachments?: string[];
  submissionDate?: string;
}

interface AchievementModeration {
  id: string;
  title: string;
  description: string;
  subtasks: AchievementSubtask[];
  assignedTo: string;
  submittedDate: string;
  reward: {
    type: 'XP' | 'G-coin';
    amount: number;
  };
  status: 'pending' | 'approved' | 'rejected';
  completionPercentage: number;
  rejectionReason?: string;
}

interface Props {
  isOpen: boolean;
  onClose: () => void;
  achievement: AchievementModeration | null;
  onApprove: (achievementId: string, subtaskId: string) => void;
  onReject: (achievementId: string, reason: string) => void;
  theme?: 'light' | 'dark';
}

export function AchievementModerationModal({ isOpen, onClose, achievement, onApprove, onReject, theme = 'light' }: Props) {
  const [rejectionModalOpen, setRejectionModalOpen] = useState(false);
  const [confirmModalOpen, setConfirmModalOpen] = useState(false);
  const [rejectionReason, setRejectionReason] = useState('');
  const [selectedSubtaskId, setSelectedSubtaskId] = useState<string | null>(null);
  const [fileViewerOpen, setFileViewerOpen] = useState(false);
  const [selectedFile, setSelectedFile] = useState<string | null>(null);

  if (!achievement) return null;

  const handleSubtaskApprove = (subtaskId: string) => {
    setSelectedSubtaskId(subtaskId);
    setConfirmModalOpen(true);
  };

  const handleConfirmApprove = () => {
    if (selectedSubtaskId) {
      onApprove(achievement.id, selectedSubtaskId);
      setConfirmModalOpen(false);
      setSelectedSubtaskId(null);
    }
  };

  const handleRejectClick = () => {
    setRejectionModalOpen(true);
  };

  const handleConfirmReject = () => {
    if (rejectionReason.trim()) {
      onReject(achievement.id, rejectionReason);
      setRejectionModalOpen(false);
      setRejectionReason('');
      onClose();
    }
  };

  const handleFileView = (file: string) => {
    setSelectedFile(file);
    setFileViewerOpen(true);
  };

  const calculateProgress = () => {
    const completedTasks = achievement.subtasks.filter(task => task.completed).length;
    return Math.round((completedTasks / achievement.subtasks.length) * 100);
  };

  return (
    <>
      <ModalOpaque
        isOpen={isOpen}
        onClose={onClose}
        title="Р В Р в‚¬Р РЋР С“Р В Р’В»Р В РЎвЂўР В Р вЂ Р В РЎвЂР РЋР РЏ Р В Р вЂ Р РЋРІР‚в„–Р В РЎвЂ”Р В РЎвЂўР В Р’В»Р В Р вЂ¦Р В Р’ВµР В Р вЂ¦Р В РЎвЂР РЋР РЏ Р В РўвЂР В РЎвЂўР РЋР С“Р РЋРІР‚С™Р В РЎвЂР В Р’В¶Р В Р’ВµР В Р вЂ¦Р В РЎвЂР РЋР РЏ"
        theme={theme}
        actions={
          <div className="flex gap-3">
            <button
              onClick={onClose}
              className="transition-colors text-center"
              style={{
                height: '44px',
                borderRadius: '12px',
                backgroundColor: theme === 'dark' ? '#202734' : '#F3F5F8',
                border: theme === 'dark' 
                  ? '1px solid rgba(255, 255, 255, 0.06)' 
                  : '1px solid #E6E9EF',
                color: theme === 'dark' ? '#E8ECF2' : '#0F172A',
                padding: '0 20px'
              }}
            >
              Р В РЎвЂєР РЋРІР‚С™Р В РЎВР В Р’ВµР В Р вЂ¦Р В РЎвЂР РЋРІР‚С™Р РЋР Р‰
            </button>
            <button
              onClick={handleRejectClick}
              className="transition-colors text-center"
              style={{
                height: '44px',
                borderRadius: '12px',
                backgroundColor: '#ff3b30',
                color: '#ffffff',
                border: 'none',
                padding: '0 20px'
              }}
            >
              Р В РЎвЂєР РЋРІР‚С™Р В РЎвЂќР В Р’В»Р В РЎвЂўР В Р вЂ¦Р В РЎвЂР РЋРІР‚С™Р РЋР Р‰
            </button>
            <button
              onClick={() => {
                // Р В РЎвЂєР В РўвЂР В РЎвЂўР В Р’В±Р РЋР вЂљР В РЎвЂР РЋРІР‚С™Р РЋР Р‰ Р В Р вЂ Р РЋР С“Р В Р’Вµ Р В Р вЂ Р РЋРІР‚в„–Р В РЎвЂ”Р В РЎвЂўР В Р’В»Р В Р вЂ¦Р В Р’ВµР В Р вЂ¦Р В Р вЂ¦Р РЋРІР‚в„–Р В Р’Вµ Р В РЎвЂ”Р В РЎвЂўР В РўвЂР В Р’В·Р В Р’В°Р В РўвЂР В Р’В°Р РЋРІР‚РЋР В РЎвЂ
                achievement.subtasks.forEach(subtask => {
                  if (subtask.hasAttachment && !subtask.completed) {
                    onApprove(achievement.id, subtask.id);
                  }
                });
                onClose();
              }}
              className="transition-colors text-center"
              style={{
                height: '44px',
                borderRadius: '12px',
                backgroundColor: '#2B82FF',
                color: '#ffffff',
                border: 'none',
                padding: '0 20px'
              }}
            >
              Р В РЎСџР РЋР вЂљР В РЎвЂР В РЎВР В Р’ВµР В Р вЂ¦Р В РЎвЂР РЋРІР‚С™Р РЋР Р‰
            </button>
          </div>
        }
      >
        <div style={{ maxHeight: '60vh', overflowY: 'auto' }}>

          <div className="flex gap-4 mb-6">
            {/* Р В Р’ВР В РЎвЂќР В РЎвЂўР В Р вЂ¦Р В РЎвЂќР В Р’В° Р В РўвЂР В РЎвЂўР РЋР С“Р РЋРІР‚С™Р В РЎвЂР В Р’В¶Р В Р’ВµР В Р вЂ¦Р В РЎвЂР РЋР РЏ */}
            <div 
              className="flex items-center justify-center flex-shrink-0"
              style={{
                width: '64px',
                height: '64px',
                backgroundColor: theme === 'dark' ? '#202734' : '#F3F5F8',
                borderRadius: '12px'
              }}
            >
              <span style={{ fontSize: '32px' }}>РЎР‚РЎСџР РЏРІР‚В </span>
            </div>
            
            {/* Р В Р’ВР В Р вЂ¦Р РЋРІР‚С›Р В РЎвЂўР РЋР вЂљР В РЎВР В Р’В°Р РЋРІР‚В Р В РЎвЂР РЋР РЏ Р В РЎвЂў Р В РўвЂР В РЎвЂўР РЋР С“Р РЋРІР‚С™Р В РЎвЂР В Р’В¶Р В Р’ВµР В Р вЂ¦Р В РЎвЂР В РЎвЂ */}
            <div className="flex-1">
              <h4 
                className="font-medium mb-2"
                style={{ color: theme === 'dark' ? '#E8ECF2' : '#0F172A' }}
              >
                {achievement.title}
              </h4>
              <p 
                className="mb-3"
                style={{ 
                  color: theme === 'dark' ? '#A7B0BD' : '#6B7280',
                  fontSize: '14px'
                }}
              >
                {achievement.description}
              </p>
              
              {/* Р В РЎСџР В РЎвЂўР В РўвЂР В Р’В·Р В Р’В°Р В РўвЂР В Р’В°Р РЋРІР‚РЋР В РЎвЂ */}
              <div className="space-y-2">
                {achievement.subtasks.map((subtask, index) => (
                  <div key={subtask.id} className="flex items-center justify-between gap-2">
                    <div className="flex items-center gap-2 flex-1">
                      <span 
                        style={{ 
                          color: theme === 'dark' ? '#E8ECF2' : '#0F172A',
                          fontSize: '14px'
                        }}
                      >
                        Р Р†Р вЂљРЎС› {subtask.description}
                      </span>
                    </div>
                    <div className="flex items-center gap-1">
                      {/* Р В РІР‚СљР В Р’В°Р В Р’В»Р В РЎвЂўР РЋРІР‚РЋР В РЎвЂќР В Р’В° Р В РўвЂР В Р’В»Р РЋР РЏ Р В РЎвЂўР В РўвЂР В РЎвЂўР В Р’В±Р РЋР вЂљР В Р’ВµР В Р вЂ¦Р В РЎвЂР РЋР РЏ */}
                      <button
                        onClick={() => handleSubtaskApprove(subtask.id)}
                        disabled={!subtask.hasAttachment || subtask.completed}
                        className="p-1 rounded transition-colors"
                        style={{
                          backgroundColor: subtask.completed 
                            ? '#34c759' 
                            : subtask.hasAttachment 
                            ? theme === 'dark' ? '#202734' : '#F3F5F8'
                            : 'transparent',
                          opacity: subtask.hasAttachment || subtask.completed ? 1 : 0.3
                        }}
                      >
                        <CheckSquare 
                          style={{ 
                            width: '16px', 
                            height: '16px',
                            color: subtask.completed ? '#ffffff' : '#34c759'
                          }} 
                        />
                      </button>
                      
                      {/* Р В Р Р‹Р В РЎвЂќР РЋР вЂљР В Р’ВµР В РЎвЂ”Р В РЎвЂќР В Р’В° Р В РўвЂР В Р’В»Р РЋР РЏ Р В РЎвЂ”Р РЋР вЂљР В РЎвЂўР РЋР С“Р В РЎВР В РЎвЂўР РЋРІР‚С™Р РЋР вЂљР В Р’В° Р РЋРІР‚С›Р В Р’В°Р В РІвЂћвЂ“Р В Р’В»Р В РЎвЂўР В Р вЂ  */}
                      <button
                        onClick={() => subtask.attachments?.[0] && handleFileView(subtask.attachments[0])}
                        disabled={!subtask.hasAttachment}
                        className="p-1 rounded transition-colors"
                        style={{
                          backgroundColor: subtask.hasAttachment 
                            ? theme === 'dark' ? '#202734' : '#F3F5F8'
                            : 'transparent',
                          opacity: subtask.hasAttachment ? 1 : 0.3
                        }}
                      >
                        <Paperclip 
                          style={{ 
                            width: '16px', 
                            height: '16px',
                            color: subtask.hasAttachment ? '#2B82FF' : theme === 'dark' ? '#A7B0BD' : '#6B7280'
                          }} 
                        />
                      </button>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          </div>

          {/* Р В РЎСџР РЋР вЂљР В РЎвЂўР В РЎвЂ“Р РЋР вЂљР В Р’ВµР РЋР С“Р РЋР С“ Р В Р вЂ Р РЋРІР‚в„–Р В РЎвЂ”Р В РЎвЂўР В Р’В»Р В Р вЂ¦Р В Р’ВµР В Р вЂ¦Р В РЎвЂР РЋР РЏ */}
          <div 
            className="mb-6 p-3 rounded-lg"
            style={{
              backgroundColor: theme === 'dark' ? 'rgba(43, 130, 255, 0.12)' : 'rgba(43, 130, 255, 0.10)'
            }}
          >
            <div className="flex items-center justify-between mb-2">
              <span 
                style={{ 
                  color: theme === 'dark' ? '#E8ECF2' : '#0F172A',
                  fontSize: '14px'
                }}
              >
                Р В РЎСџР РЋР вЂљР В РЎвЂўР В РЎвЂ“Р РЋР вЂљР В Р’ВµР РЋР С“Р РЋР С“ Р В Р вЂ Р РЋРІР‚в„–Р В РЎвЂ”Р В РЎвЂўР В Р’В»Р В Р вЂ¦Р В Р’ВµР В Р вЂ¦Р В РЎвЂР РЋР РЏ
              </span>
              <span 
                style={{ 
                  color: theme === 'dark' ? '#E8ECF2' : '#0F172A',
                  fontSize: '14px'
                }}
              >
                {calculateProgress()}%
              </span>
            </div>
            <div 
              className="w-full rounded-full"
              style={{
                height: '8px',
                backgroundColor: theme === 'dark' ? '#202734' : '#E6E9EF'
              }}
            >
              <div 
                className="rounded-full transition-all duration-300"
                style={{ 
                  width: `${calculateProgress()}%`,
                  height: '8px',
                  backgroundColor: '#2B82FF'
                }}
              />
            </div>
          </div>

          {/* Р В Р’ВР В Р вЂ¦Р РЋРІР‚С›Р В РЎвЂўР РЋР вЂљР В РЎВР В Р’В°Р РЋРІР‚В Р В РЎвЂР РЋР РЏ Р В РЎвЂў Р В РЎвЂ”Р В РЎвЂўР В РўвЂР В Р’В°Р РЋРІР‚РЋР В Р’Вµ */}
          <div 
            className="mb-6 space-y-1"
            style={{
              color: theme === 'dark' ? '#A7B0BD' : '#6B7280',
              fontSize: '12px'
            }}
          >
            <div>Р В Р Р‹Р В РЎвЂўР РЋРІР‚С™Р РЋР вЂљР РЋРЎвЂњР В РўвЂР В Р вЂ¦Р В РЎвЂР В РЎвЂќ: {achievement.assignedTo}</div>
            <div>Р В РІР‚СњР В Р’В°Р РЋРІР‚С™Р В Р’В° Р В РЎвЂ”Р В РЎвЂўР В РўвЂР В Р’В°Р РЋРІР‚РЋР В РЎвЂ: {new Date(achievement.submittedDate).toLocaleDateString('ru-RU')}</div>
            <div>Р В РЎСљР В Р’В°Р В РЎвЂ“Р РЋР вЂљР В Р’В°Р В РўвЂР В Р’В°: {achievement.reward.amount} {achievement.reward.type}</div>
          </div>
        </div>
      </ModalOpaque>

      {/* Р В РЎС™Р В РЎвЂўР В РўвЂР В Р’В°Р В Р’В»Р РЋР Р‰Р В Р вЂ¦Р В РЎвЂўР В Р’Вµ Р В РЎвЂўР В РЎвЂќР В Р вЂ¦Р В РЎвЂў Р В РЎвЂ”Р В РЎвЂўР В РўвЂР РЋРІР‚С™Р В Р вЂ Р В Р’ВµР РЋР вЂљР В Р’В¶Р В РўвЂР В Р’ВµР В Р вЂ¦Р В РЎвЂР РЋР РЏ Р В РЎвЂўР В РўвЂР В РЎвЂўР В Р’В±Р РЋР вЂљР В Р’ВµР В Р вЂ¦Р В РЎвЂР РЋР РЏ */}
      <ModalOpaque
        isOpen={confirmModalOpen}
        onClose={() => setConfirmModalOpen(false)}
        title="Р В РЎСџР В РЎвЂўР В РўвЂР РЋРІР‚С™Р В Р вЂ Р В Р’ВµР РЋР вЂљР В Р’В¶Р В РўвЂР В Р’ВµР В Р вЂ¦Р В РЎвЂР В Р’Вµ Р В Р вЂ Р РЋРІР‚в„–Р В РЎвЂ”Р В РЎвЂўР В Р’В»Р В Р вЂ¦Р В Р’ВµР В Р вЂ¦Р В РЎвЂР РЋР РЏ"
        theme={theme}
        actions={
          <div className="flex gap-3">
            <button
              onClick={() => setConfirmModalOpen(false)}
              className="transition-colors text-center"
              style={{
                height: '44px',
                borderRadius: '12px',
                backgroundColor: theme === 'dark' ? '#202734' : '#F3F5F8',
                border: theme === 'dark' 
                  ? '1px solid rgba(255, 255, 255, 0.06)' 
                  : '1px solid #E6E9EF',
                color: theme === 'dark' ? '#E8ECF2' : '#0F172A',
                padding: '0 20px'
              }}
            >
              Р В РЎСљР В Р’ВµР РЋРІР‚С™
            </button>
            <button
              onClick={handleConfirmApprove}
              className="transition-colors text-center"
              style={{
                height: '44px',
                borderRadius: '12px',
                backgroundColor: '#2B82FF',
                color: '#ffffff',
                border: 'none',
                padding: '0 20px'
              }}
            >
              Р В РІР‚СњР В Р’В°
            </button>
          </div>
        }
      >
        <div className="text-center">
          <p 
            style={{ 
              color: theme === 'dark' ? '#A7B0BD' : '#6B7280',
              fontSize: '14px'
            }}
          >
            Р В РІР‚в„ўР РЋРІР‚в„– Р РЋРЎвЂњР В Р вЂ Р В Р’ВµР РЋР вЂљР В Р’ВµР В Р вЂ¦Р РЋРІР‚в„–, Р РЋРІР‚РЋР РЋРІР‚С™Р В РЎвЂў Р В Р’В·Р В Р’В°Р В РўвЂР В Р’В°Р В Р вЂ¦Р В РЎвЂР В Р’Вµ Р В Р вЂ Р РЋРІР‚в„–Р В РЎвЂ”Р В РЎвЂўР В Р’В»Р В Р вЂ¦Р В Р’ВµР В Р вЂ¦Р В РЎвЂў Р В Р вЂ Р В Р’ВµР РЋР вЂљР В Р вЂ¦Р В РЎвЂў?
          </p>
        </div>
      </ModalOpaque>

      {/* Р В РЎС™Р В РЎвЂўР В РўвЂР В Р’В°Р В Р’В»Р РЋР Р‰Р В Р вЂ¦Р В РЎвЂўР В Р’Вµ Р В РЎвЂўР В РЎвЂќР В Р вЂ¦Р В РЎвЂў Р В РЎвЂўР РЋРІР‚С™Р В РЎвЂќР В Р’В»Р В РЎвЂўР В Р вЂ¦Р В Р’ВµР В Р вЂ¦Р В РЎвЂР РЋР РЏ */}
      <ModalOpaque
        isOpen={rejectionModalOpen}
        onClose={() => setRejectionModalOpen(false)}
        title="Р В РЎСџР РЋР вЂљР В РЎвЂР РЋРІР‚РЋР В РЎвЂР В Р вЂ¦Р В Р’В° Р В РЎвЂўР РЋРІР‚С™Р В РЎвЂќР В Р’В»Р В РЎвЂўР В Р вЂ¦Р В Р’ВµР В Р вЂ¦Р В РЎвЂР РЋР РЏ"
        theme={theme}
        actions={
          <div className="flex gap-3">
            <button
              onClick={() => setRejectionModalOpen(false)}
              className="transition-colors text-center"
              style={{
                height: '44px',
                borderRadius: '12px',
                backgroundColor: theme === 'dark' ? '#202734' : '#F3F5F8',
                border: theme === 'dark' 
                  ? '1px solid rgba(255, 255, 255, 0.06)' 
                  : '1px solid #E6E9EF',
                color: theme === 'dark' ? '#E8ECF2' : '#0F172A',
                padding: '0 20px'
              }}
            >
              Р В РЎвЂєР РЋРІР‚С™Р В РЎВР В Р’ВµР В Р вЂ¦Р В Р’В°
            </button>
            <button
              onClick={handleConfirmReject}
              disabled={!rejectionReason.trim()}
              className="transition-colors text-center disabled:opacity-50 disabled:cursor-not-allowed"
              style={{
                height: '44px',
                borderRadius: '12px',
                backgroundColor: rejectionReason.trim() ? '#ff3b30' : theme === 'dark' ? '#202734' : '#E6E9EF',
                color: '#ffffff',
                border: 'none',
                padding: '0 20px'
              }}
            >
              Р В РЎвЂєР РЋРІР‚С™Р В РЎвЂ”Р РЋР вЂљР В Р’В°Р В Р вЂ Р В РЎвЂР РЋРІР‚С™Р РЋР Р‰
            </button>
          </div>
        }
      >
        <div>
          <textarea
            placeholder="Р В Р в‚¬Р В РЎвЂќР В Р’В°Р В Р’В¶Р В РЎвЂР РЋРІР‚С™Р В Р’Вµ, Р РЋРІР‚РЋР РЋРІР‚С™Р В РЎвЂў Р В Р вЂ Р РЋРІР‚в„–Р В РЎвЂ”Р В РЎвЂўР В Р’В»Р В Р вЂ¦Р В Р’ВµР В Р вЂ¦Р В РЎвЂў Р В Р вЂ¦Р В Р’ВµР В РЎвЂ”Р РЋР вЂљР В Р’В°Р В Р вЂ Р В РЎвЂР В Р’В»Р РЋР Р‰Р В Р вЂ¦Р В РЎвЂў..."
            value={rejectionReason}
            onChange={(e) => setRejectionReason(e.target.value)}
            className="w-full transition-colors resize-none"
            rows={4}
            style={{
              height: '100px',
              borderRadius: '12px',
              backgroundColor: theme === 'dark' ? '#202734' : '#F3F5F8',
              border: theme === 'dark' 
                ? '1px solid rgba(255, 255, 255, 0.06)' 
                : '1px solid #E6E9EF',
              color: theme === 'dark' ? '#E8ECF2' : '#0F172A',
              padding: '12px',
              fontSize: '14px',
              outline: 'none'
            }}
          />
        </div>
      </ModalOpaque>

      {/* Р В РЎС™Р В РЎвЂўР В РўвЂР В Р’В°Р В Р’В»Р РЋР Р‰Р В Р вЂ¦Р В РЎвЂўР В Р’Вµ Р В РЎвЂўР В РЎвЂќР В Р вЂ¦Р В РЎвЂў Р В РЎвЂ”Р РЋР вЂљР В РЎвЂўР РЋР С“Р В РЎВР В РЎвЂўР РЋРІР‚С™Р РЋР вЂљР В Р’В° Р РЋРІР‚С›Р В Р’В°Р В РІвЂћвЂ“Р В Р’В»Р В Р’В° */}
      <ModalOpaque
        isOpen={fileViewerOpen}
        onClose={() => setFileViewerOpen(false)}
        title="Р В РЎСџР РЋР вЂљР В РЎвЂўР РЋР С“Р В РЎВР В РЎвЂўР РЋРІР‚С™Р РЋР вЂљ Р РЋРІР‚С›Р В Р’В°Р В РІвЂћвЂ“Р В Р’В»Р В Р’В°"
        theme={theme}
        actions={
          <button
            onClick={() => setFileViewerOpen(false)}
            className="w-full transition-colors text-center"
            style={{
              height: '44px',
              borderRadius: '12px',
              backgroundColor: '#2B82FF',
              color: '#ffffff',
              border: 'none'
            }}
          >
            Р В РІР‚вЂќР В Р’В°Р В РЎвЂќР РЋР вЂљР РЋРІР‚в„–Р РЋРІР‚С™Р РЋР Р‰
          </button>
        }
      >
        <div 
          className="text-center p-8 rounded-lg mb-4"
          style={{
            backgroundColor: theme === 'dark' ? 'rgba(43, 130, 255, 0.12)' : 'rgba(43, 130, 255, 0.10)'
          }}
        >
          <div style={{ fontSize: '64px', marginBottom: '16px' }}>РЎР‚РЎСџРІР‚СљРІР‚С›</div>
          <p 
            className="mb-2"
            style={{ 
              fontSize: '14px',
              color: theme === 'dark' ? '#A7B0BD' : '#6B7280'
            }}
          >
            Р В Р’В¤Р В Р’В°Р В РІвЂћвЂ“Р В Р’В»: {selectedFile}
          </p>
          <p 
            style={{ 
              fontSize: '12px',
              color: theme === 'dark' ? '#A7B0BD' : '#6B7280'
            }}
          >
            Р В РЎСџР РЋР вЂљР В Р’ВµР В РўвЂР В Р вЂ Р В Р’В°Р РЋР вЂљР В РЎвЂР РЋРІР‚С™Р В Р’ВµР В Р’В»Р РЋР Р‰Р В Р вЂ¦Р РЋРІР‚в„–Р В РІвЂћвЂ“ Р В РЎвЂ”Р РЋР вЂљР В РЎвЂўР РЋР С“Р В РЎВР В РЎвЂўР РЋРІР‚С™Р РЋР вЂљ Р РЋРІР‚С›Р В Р’В°Р В РІвЂћвЂ“Р В Р’В»Р В Р’В°
          </p>
        </div>
      </ModalOpaque>
    </>
  );
}
