import React from 'react';

interface BackgroundFXProps {
  theme: 'light' | 'dark';
  isHomePage?: boolean;
}

export const BackgroundFX: React.FC<BackgroundFXProps> = ({ theme, isHomePage = false }) => {
  if (isHomePage && theme === 'light') {
    // Р С™РЎР‚Р В°РЎРѓР С‘Р Р†РЎвЂ№Р в„– Р С–РЎР‚Р В°Р Т‘Р С‘Р ВµР Р…РЎвЂљР Р…РЎвЂ№Р в„– РЎвЂћР С•Р Р… Р Т‘Р В»РЎРЏ Р С–Р В»Р В°Р Р†Р Р…Р С•Р в„– РЎРѓРЎвЂљРЎР‚Р В°Р Р…Р С‘РЎвЂ РЎвЂ№ РЎРѓ РЎРЊРЎвЂћРЎвЂћР ВµР С”РЎвЂљР С•Р С Р Р…Р ВµР В±Р В°
    return (
      <div className="fixed inset-0 background-fx">
        {/* Р С›РЎРѓР Р…Р С•Р Р†Р Р…Р С•Р в„– Р С–РЎР‚Р В°Р Т‘Р С‘Р ВµР Р…РЎвЂљР Р…РЎвЂ№Р в„– РЎРѓР В»Р С•Р в„– */}
        <div 
          className="absolute inset-0"
          style={{
            background: `linear-gradient(180deg, 
              rgba(59, 130, 246, 0.8) 0%, 
              rgba(59, 130, 246, 0.4) 30%, 
              rgba(255, 255, 255, 0.9) 70%, 
              #F8FAFC 100%
            )`,
            zIndex: 1,
            pointerEvents: 'none'
          }}
        />
        
        {/* Р вЂќР ВµР С”Р С•РЎР‚Р В°РЎвЂљР С‘Р Р†Р Р…РЎвЂ№Р в„– РЎРѓР С‘Р Р…Р С‘Р в„– РЎРЊРЎвЂћРЎвЂћР ВµР С”РЎвЂљ Р Р…Р ВµР В±Р В° */}
        <div 
          className="absolute"
          style={{
            top: '-10%',
            right: '-5%',
            width: '50%',
            height: '70%',
            background: `linear-gradient(180deg, rgba(59, 130, 246, 0.8) 0%, rgba(59, 130, 246, 0) 100%)`,
            opacity: 0.2,
            filter: 'blur(48px)',
            borderRadius: '50%',
            zIndex: 2,
            pointerEvents: 'none'
          }}
        />
        
        {/* Р вЂќР С•Р С—Р С•Р В»Р Р…Р С‘РЎвЂљР ВµР В»РЎРЉР Р…РЎвЂ№Р в„– РЎРѓР В»Р С•Р в„– Р Т‘Р В»РЎРЏ Р С–Р В»РЎС“Р В±Р С‘Р Р…РЎвЂ№ */}
        <div 
          className="absolute inset-0"
          style={{
            background: `radial-gradient(ellipse at center top, 
              rgba(59, 130, 246, 0.1) 0%, 
              transparent 60%
            )`,
            zIndex: 3,
            pointerEvents: 'none'
          }}
        />
      </div>
    );
  }

  if (isHomePage && theme === 'dark') {
    // Р СћР ВµР СР Р…Р В°РЎРЏ РЎвЂљР ВµР СР В° Р Т‘Р В»РЎРЏ Р С–Р В»Р В°Р Р†Р Р…Р С•Р в„– РЎРѓРЎвЂљРЎР‚Р В°Р Р…Р С‘РЎвЂ РЎвЂ№
    return (
      <div className="fixed inset-0 background-fx">
        <div 
          className="absolute inset-0"
          style={{
            background: `linear-gradient(180deg, 
              rgba(0, 132, 255, 0.15) 0%, 
              rgba(18, 21, 27, 0.9) 30%, 
              #12151B 100%
            )`,
            zIndex: 1,
            pointerEvents: 'none'
          }}
        />
      </div>
    );
  }

  // Р С›Р В±РЎвЂ№РЎвЂЎР Р…РЎвЂ№Р Вµ РЎРѓРЎвЂљРЎР‚Р В°Р Р…Р С‘РЎвЂ РЎвЂ№
  if (theme === 'light') {
    return (
      <div className="fixed inset-0 background-fx">
        {/* Р С›РЎРѓР Р…Р С•Р Р†Р Р…Р С•Р в„– РЎвЂћР С•Р Р… */}
        <div 
          className="absolute inset-0"
          style={{
            background: '#F8FAFC',
            zIndex: 1,
            pointerEvents: 'none'
          }}
        />
        
        {/* Р вЂќР ВµР С”Р С•РЎР‚Р В°РЎвЂљР С‘Р Р†Р Р…РЎвЂ№Р в„– РЎРѓР С‘Р Р…Р С‘Р в„– РЎРЊРЎвЂћРЎвЂћР ВµР С”РЎвЂљ Р Р…Р ВµР В±Р В° */}
        <div 
          className="absolute"
          style={{
            top: '-10%',
            right: '-5%',
            width: '50%',
            height: '70%',
            background: `linear-gradient(180deg, rgba(59, 130, 246, 0.8) 0%, rgba(59, 130, 246, 0) 100%)`,
            opacity: 0.15,
            filter: 'blur(48px)',
            borderRadius: '50%',
            zIndex: 2,
            pointerEvents: 'none'
          }}
        />
      </div>
    );
  }

  // Р СћР ВµР СР Р…Р В°РЎРЏ РЎвЂљР ВµР СР В° Р Т‘Р В»РЎРЏ Р С•Р В±РЎвЂ№РЎвЂЎР Р…РЎвЂ№РЎвЂ¦ РЎРѓРЎвЂљРЎР‚Р В°Р Р…Р С‘РЎвЂ 
  return (
    <div className="fixed inset-0 background-fx">
      <div 
        className="absolute inset-0"
        style={{
          background: '#12151B',
          zIndex: 1,
          pointerEvents: 'none'
        }}
      />
    </div>
  );
};