# 🚀 Инструкция по запуску Grither App

## ❌ КРИТИЧЕСКИЕ ПРОБЛЕМЫ, КОТОРЫЕ НУЖНО ИСПРАВИТЬ:

### 1. Установка Node.js
**ОБЯЗАТЕЛЬНО!** Скачайте и установите Node.js:
- Перейдите на https://nodejs.org/
- Скачайте LTS версию (20.x или 18.x)
- Установите с настройками по умолчанию
- Перезапустите терминал

### 2. Проверка установки
```bash
node --version
npm --version
```

### 3. Установка зависимостей
```bash
# В корневой папке проекта
npm run install:all
```

### 4. Запуск приложения
```bash
# Запуск frontend и backend одновременно
npm run dev

# Или по отдельности:
npm run dev:frontend  # Frontend на http://localhost:5173
npm run dev:backend   # Backend на http://localhost:3001
```

## 🔧 Исправленные проблемы:

1. ✅ Исправлен vite.config.ts - убраны избыточные алиасы
2. ✅ Создан простой backend сервер
3. ✅ Исправлены TypeScript ошибки
4. ✅ Очищена конфигурация

## 🌐 Доступ к приложению:

После запуска `npm run dev`:
- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:3001

## 🐛 Если что-то не работает:

1. Убедитесь что Node.js установлен
2. Очистите кэш: `npm cache clean --force`
3. Удалите node_modules: `rm -rf node_modules frontend/node_modules backend/node_modules`
4. Переустановите: `npm run install:all`
5. Запустите: `npm run dev`
